function printUserBadge(name: string, level: number): void {
  // your code function printUserBadge(name: string, level: number): void {
  const badge = `
===== USER BADGE =====
Nombre: ${name}
Nivel: ${level}
======================
  `;

  console.log(badge);
}
  
  }

export {};
